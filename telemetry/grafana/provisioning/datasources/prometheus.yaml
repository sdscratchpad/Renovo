apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus          # <-- add this line
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
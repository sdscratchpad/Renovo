apiVersion: v1
kind: Service
metadata:
  name: sample-app
  namespace: workloads
  labels:
    app: sample-app
spec:
  type: NodePort
  selector:
    app: sample-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
